<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.udfex.ams.module.account.mapper.UserMapper" >

    <!--    查询消息    -->
    <select id="getUsers" resultType="com.udfex.ams.module.account.model.User" parameterType="java.util.HashMap">
        SELECT
        u.id AS userId,
        u.username,
        u.email,
        u.locked
        FROM
        sys_users u
        <where>
              <if test="keyword">
                  AND u.username LIKE CONCAT('%', #{keyword}, '%') or u.email like CONCAT('%', #{keyword}, '%')
              </if>
              <if test="id">
                  AND m.ID = #{id}
              </if>
          </where>
    </select>

    <!--    查询关联关系表    -->
    <select id="findUsersByRolesId" resultType="com.udfex.ams.module.account.web.controller.vo.UserGroupRelation" parameterType="java.util.HashMap">
        SELECT
        u.ID AS user_id,
        u.USERNAME,
        u.EMAIL
        FROM
        sys_users u
        <if test="id and chose">
            LEFT JOIN sys_users_roles ur
            ON u.ID = ur.USER_ID
        </if>
        <where>
            <if test="id and chose">
                AND ur.ROLE_ID = #{id}
            </if>
            <if test="!chose">
                AND u.ID NOT IN
                (SELECT
                u.ID AS user_id
                FROM
                sys_users u
                LEFT JOIN sys_users_roles ur
                ON u.ID = ur.USER_ID
                WHERE ur.ROLE_ID= #{id})
            </if>
        </where>
    </select>
</mapper>